# Publishes the PWA to GitHub Pages. Free for public repositories, and free
# for private ones on any paid plan — if this repo stays private on the free
# plan, run `pnpm build` locally and host apps/app/dist anywhere static
# instead. Nothing about the app depends on where it is served from.
name: Deploy PWA

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # No version pinned here on purpose: the root package.json declares
      # `packageManager`, and specifying both makes the action fail outright
      # rather than pick one.
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: apps/app/dist
      - id: deployment
        uses: actions/deploy-pages@v4
